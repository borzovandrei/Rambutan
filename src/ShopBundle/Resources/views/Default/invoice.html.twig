<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html" charset="utf-8"/>
    <title>Чек</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css"
              integrity="sha384-/Y6pD6FV/Vv2HJnA6t+vslU6fwYXjCFtcEpHbNJ0lyAFsXTsjBbfaDjzALeQsN6M"
              crossorigin="anonymous">

</head>
<body>
    <div class="row">
        <div class="col-lg-5 col-md-6">
            <div style="text-align: center;">
            <img src="{{ rootDir ~ '/web' ~ asset('img/logo.png') }}"  width="50" height="50"/>
            <h1>Rambutan: online shop</h1>
            </div>
            <div class="container">
                <h3>Информация о заказе:</h3>
                <table class="table">
                    <tbody>
                    <tr>
                        <th scope="row">Номер заказа:</th>
                        <td>{{ order.id }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Имя Фамилия:</th>
                        <td>{{ order.lastname }} {{ order.firstname }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Телефон:</th>
                        <td>{{ order.phone }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Дата оформления:</th>
                        <td> {{ order.created|date('d.m.Y') }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Дата поставки:</th>
                        <td> {{ order.date|date('d.m.Y') }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Время поставки:</th>
                        <td> {{ order.date|date('h:m') }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Адрес:</th>
                        <td>{{ order.address }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Комментарий:</th>
                        <td>{{ order.comment}}</td>
                    </tr>
                    <tr>
                        <th scope="row">Цена:</th>
                        <td>{{ order.price }} руб.</td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="col-lg-6 col-md-6">
            <h3>Купленные продукты</h3>
            <table class="table table-condensed">
                <thead>
                <tr>
                    <th>Изображение</th>
                    <th>Артикул</th>
                    <th>Продукт</th>
                    <th>Цена</th>
                    <th>Колличество</th>
                </tr>
                </thead>
                <tbody>
                {% if (products) %}
                    {% for key, products in products %}
                        <tr>
                            <td><img style="text-align: center;"
                                     src="{{ rootDir ~ '/web' ~ asset(['img/products/', products.path]|join) }}"
                                     alt="not found" width="40" height="40"></td>
                            <td>№: {{ products.id }}</td>
                            <td>{{ products.name }}</td>
                            <td>{{ products.shopprice }} руб.</td>
                            <td> {{ sum[key] }} {{ products.measure.name }}.</td>
                        </tr>
                    {% endfor %}
                {% else %}
                    <p>Мы не нашли товары в данном заказе.</p>
                {% endif %}
                </tbody>
            </table>
            <div style="text-align: right;">
                <img src="{{ rootDir ~ '/web' ~ asset('img/post.png') }}"  width="200" />
            </div>
        </div>
    </div>
</body>