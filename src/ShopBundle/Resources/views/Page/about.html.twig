{% extends 'ShopBundle:Default:index.html.twig' %}

{% block title %}О компании{% endblock %}

{% block body %}
    <div class="container jumbotron border border-success ">
        <div class="container ">
            {% for flashMessage in app.session.flashbag.get('shop-notice') %}
                <div class="shop-notice">
                    {{ flashMessage }}
                </div>
            {% endfor %}
            <h3>Мы продаем только свежие продукты !</h3>
            <div class="row" style="justify-content:center;">
                <div class="col-sm-6  center-block" style="padding-bottom: 50px;">
                    <div class="card-group">
                        <a class="card" style="margin-right: 10px;"  href="https://vk.com/write-162389203">
                            <img class="card-img-top" src="img/vk.png" alt="ВКонтакте">
                            <div class="card-body">
                                <p class="card-title">Мы "ВКонтакте"</p>
                            </div>
                        </a>
                        <a class="card" style="margin-left: 10px;"  href="https://t.me/RambutanShopBot">
                            <img class="card-img-top" src="img/telegram.jpg" alt="Telegram">
                            <div class="card-body">
                                <p class="card-title">Мы в "Telegram"</p>
                            </div>
                        </a>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-sm-4">
                    <div class="container">

                    </div>
                </div>
                <div class="col-sm-4">
                    <p>Есть вопросы? Свяжитесь с нами!</p>
                    {{ form_start(form, { 'action': path('shop_about'), 'method': 'POST', 'attr': {'class': 'blogger'} }) }}
                    {{ form_errors(form) }}
                    {{ form_row(form.name) }}
                    {{ form_row(form.email) }}
                    {{ form_row(form.subject) }}
                    {{ form_row(form.body) }}
                    {{ form_rest(form) }}
                    <input type="submit" value="Submit" class="btn btn-success"/>
                </div>
            </div>
        </div>
    </div>

{% endblock %}