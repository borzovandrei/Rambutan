{% extends 'ShopBundle:Default:index.html.twig' %}
{% block body %}
    <div class="container">
        <h2>Корзина</h2>
        <p>Вы можете добавить или удалить товар:</p>
        <table class="table table-condensed">
            <thead>
            <tr>
                <th>Изображение</th>
                <th>Продукт</th>
                <th>Цена</th>
                <th>Колличество</th>
                <th>Изменить</th>
            </tr>
            </thead>
            <tbody>
            {% for cart in carts %}
                <tr>
                    <td><img class="rounded"
                             src="{{ asset(['img/products/', cart.path]|join) }}"
                             alt="Generic placeholder image" width="50" height=50></td>
                    <td>{{ cart.name }}</td>
                    <td>{{ cart.shopprice }}руб.</td>

                    <td> <sum class="sum{{ cart.id }}" id='id{{ cart.id }}' sum-id='{{ cart.id }}'>{{ result[cart.id] }}</sum> {{ cart.measure.name }}.</td>

                    <td>
                        <a class="btn btn-success btn-plus"
                           data-id="{{ cart.id }}"
                           data-price="{{ cart.shopPrice }}"
                           data-sum="{{ result[cart.id] }}"
                           data-url="{{ path('add_cart') }}"
                           role="button"><img class="icons"
                                              src="{{ asset('img/icons/plus.svg') }}"
                                              alt="О компании"
                                              width="20" height="20"></a>

                        <a class="btn btn-warning btn-minus"
                           data-id="{{ cart.id }}"
                           data-price="{{ cart.shopPrice }}"
                           data-sum="{{ result[cart.id] }}"
                           data-url="{{ path('minus_cart') }}"
                           role="button"><img class="icons"
                                              src="{{ asset('img/icons/minus.svg') }}"
                                              alt="О компании"
                                              width="20" height="20"></a>

                        <a class="btn btn-danger btn-del"
                           data-id="{{ cart.id }}"
                           data-sum="{{ result[cart.id] }}"
                           data-price="{{ cart.shopPrice }}"
                           data-url="{{ path('del_cart') }}"
                           role="button"><img class="icons"
                                              src="{{ asset('img/icons/garbage.svg') }}"
                                              alt="О компании"
                                              width="20" height="20"></a>
                    </td>
                </tr>
            {% else %}
                <p>Положите товар в корзину</p>
            {% endfor %}
            </tbody>
        </table>
        <br>Итого в вашей корзине продуктов на <b id="sum_cart">{{ sum }}руб.</b>
    </div>
    <td><a class="btn btn-secondary" role="button" href="{{ path('shop_order') }}">Оформить заказ</a></td>

{% endblock %}